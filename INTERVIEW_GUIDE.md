# 灵枢医疗多智能体 — 面试话术指南

> 基于实际项目代码验证，所有技术细节均可追溯源码。标注 ⚠️ 的为已知限制，面试时坦诚说明。

---

## 一、开场定调（30 秒）

"医疗数据处理的核心矛盾不是单一格式的技术难度，而是**多源异构数据在同一场景下需要协同处理**。一个完整的患者画像需要同时消费 HIS 的结构化处方、LIS 的半结构化检验报告、PACS 的二进制 DICOM 影像、EMR 的扫描版 PDF 病历——它们的格式、编码、查询方式和错误模式完全不同。"

---

## 二、按数据层级递进讲解

### 第一层：结构化数据（"看起来容易，其实有坑"）

| 讲什么 | 怎么讲 |
|--------|--------|
| 数据源 | HIS 的药品表、患者档案、挂号记录——10 张 MySQL 表 |
| 难点 | 不是格式问题，是**语义对齐**。HIS 的"科室编码"、LIS 的"检验项目编码"、EMR 的"ICD-10 诊断编码"是三套完全不同的编码体系，一个患者从挂号到检查到诊断，数据需要在三套编码之间对齐 |
| 项目体现 | 10 张 MySQL 表 + 4 个数据适配器（HIS/LIS/EMR/PACS），每个适配器封装一套数据访问逻辑。NL2SQL 引擎通过 **4 层防护**（Prompt 约束 → 正则黑名单 → 敏感字段过滤 → 结果脱敏，加上 EXPLAIN 语法验证共 5 道防线），让不懂 SQL 的管理员也能安全查询数据 |
| 面试加分句 | "你可能觉得结构化数据好处理，但医疗的结构化数据有一个独特问题：**它不是为 AI 设计的，而是为医保结算和绩效考核设计的**。'主诉'字段经常被医生写成'腹痛3天'——这个字段在 HIS 里是 VARCHAR，你以为结构化了吗？没有，你得先做 NER 提取'腹痛'和'3天'两个实体" |

### 第二层：半结构化文本（"LLM 不万能"）

| 讲什么 | 怎么讲 |
|--------|--------|
| 数据源 | 药品说明书 .txt、临床指南 .txt、检验报告数字串——格式统一但内容自由 |
| 难点 | 同一个症状在 30 份文档中以 15 种方式描述。传统向量检索只能做"表面相似"，无法推理"A 药和 B 药都通过肾脏排泄 → 有潜在冲突" |
| 项目体现 | **三层症状匹配管道**：① LLM 口语→术语标准化（15 条预定义映射规则）；② Neo4j 精确匹配；③ Milvus 语义兜底（余弦相似度 > 0.85）。三层不是做同一件事，而是**各自兜上一层的漏**。**MS GraphRAG**：对 30 份文档做社区检测 + 社区摘要（Leiden 算法，由 `graphrag` pip 包提供，本项目封装查询端），让 LLM 理解"整个文档集的主题"而不仅是"哪一篇相关" |
| 面试加分句 | "很多人以为 LLM 检索就是 embed + cosine。但医疗文档有个致命问题：**短查询 vs 长文档的表示鸿沟**。用户说'血压高'，文档写的是'原发性高血压的一线药物治疗方案包括ACEI和CCB类'——向量相似度很低但语义高度相关。我们的方案是 HyDE（假设文档嵌入）：先让 LLM 把'血压高'展开成一段假设回答，再用这段假设回答去做检索。**坦诚地说，本项目实现了 HyDE，但没有对检索质量做量化评估**——原论文报告召回率提升 15-30%，我们也需要医生标注数据集来验证" |

### 第三层：PDF / 扫描件（"格式即挑战"）

| 讲什么 | 怎么讲 |
|--------|--------|
| 数据源 | 印刷版 PDF 药品说明书、扫描版历史病历（项目中：5 份 PDF 诊断报告/检验报告） |
| 难点 | PDF 是"纸的数字化模拟"，不是"数据的结构化容器"。表格在 PDF 中是像素排列，不是 HTML `<table>`。印章覆盖在文字上面——OCR 要么漏掉文字，要么把印章当文字识别 |
| 项目体现 | MinerU pipeline 引擎（布局分析 → 区域裁剪 → PP-OCRv6 文字识别 → 公式→LaTeX → 表格→HTML → 阅读顺序拼接），不是简单地 `PyPDF2.read()`。**MinerU 官方在 OmniDocBench v1.6 上报告 pipeline 后端 86.2 分——本项目直接调用 CLI，未做独立的解析准确率评估** |
| 面试加分句 | "MinerU 的 pipleline 后端证明了一点：在文档解析这个场景下，不是模型越大越好。**工程化的特征工程（布局分析 + 区域裁剪 + 分模型处理）比一个端到端的 VLM 更稳定**，因为它不会幻想——VLM 有时会'看到'图片中没有的文字。" |

### 第四层：图数据 / 多跳关系（"SQL 解决不了的问题"）

| 讲什么 | 怎么讲 |
|--------|--------|
| 数据源 | Neo4j 7 类节点 + 8 种关系（疾病-症状-药品-科室-检查-食物-厂商） |
| 难点 | "阿莫西林和布洛芬能一起吃吗？"——这不是一个模糊查询。SQL 需要 4 表 JOIN 才能回答，且无法发现"间接"关系（如两者都经肾脏排泄，同服增加肾损伤风险——这种关系在 stored data 里没有显式链接） |
| 项目体现 | **NL2Cypher 5 层验证**：① Prompt 约束（注入 Schema + 合法标签/关系列表）；② 格式检查（非 MATCH 开头 → 拒绝）；③ **标签/关系白名单**（纯正则，零成本，拦截 LLM 幻觉标签，弥补 EXPLAIN 不校验 schema-optional 的 Neo4j）；④ EXPLAIN 预编译（Neo4j 语法校验）；⑤ **语义回译校验**（Cypher→NL→判断是否匹配用户意图）。第⑤层是 soft-warning——不匹配时记录日志但不拦截，因为 LLM-as-Judge 自身准确率未经评估 |
| 面试加分句 | "NL2Cypher 最大的坑不是语法错误——EXPLAIN 能拦住语法错误。**真正的问题是你问'糖尿病常用药'，LLM 生成了查症状的 Cypher——语法正确，语义错误。**我们的回译校验层专门针对这个问题：执行 Cypher 后用 LLM 重新翻译回自然语言，再判断'这条查询回答的是用户的问题吗'？如果不匹配，带着错误信息重试。之所以是 soft-warning 不是 hard-reject：在缺乏实验数据时直接上线 hard-reject 比不校验更危险——可能把正确查询误判为错误。" |

---

## 三、多源协同查询（"最后的压轴"）

| 讲什么 | 怎么讲 |
|--------|--------|
| 核心观点 | "最后压轴的难题不是任何一个单点技术，而是**如何让一个入口同时访问 4 种异构数据源**。" |
| 项目体现 | Supervisor Agent — 意图路由器（6 类意图，单意图输出，多 Agent 串联由 Supervisor System Prompt 驱动）→ 5 个 Agent → 7 个 Engine → 4 个 Adapter。用户说"我头痛发烧，吃什么药"，Supervisor 先 search_memory 查患者历史（Milvus），再调 Drug Agent 查 HISAdapter + GraphRAG，最后 save_memory（Milvus）——中间涉及关系库、图库、模拟 HIS，但用户看到的是一个自然回答 |
| 面试加分句 | "很多多智能体系统的问题是**Agent 之间信息传递断裂**。我们的方案是 Supervisor 在调用每个子 Agent 前先 search_memory，调用后 save_memory——这样每个 Agent 都能获取患者的历史过敏史和既往诊断，不会出现'第二个 Agent 重复问了第一个 Agent 已经知道的事情'。但**诚实地讲**，目前这个机制是通过 System Prompt 驱动的，LLM 理论上可以不遵——不是硬编码强制的。" |

---

## 四、项目量化指标

| 指标 | 数值 | 评估方式 |
|------|:--:|------|
| 单元测试 | **110 个** | pytest 全量通过 |
| 评估维度 | **4 个** | 急症检测 / 意图分类 / SQL 安全 / 越界检测（共 13 个评估测试） |
| 急症检出率 | 直接关键词 **100%**，含委婉表达 **≥95%** | IntentRouter 正则匹配（28 个关键词，纯 CPU，<1ms） |
| SQL 危险拒绝率 | **100%** | 15 条测试（DROP/DELETE/INSERT/UPDATE/敏感字段） |
| NL2Cypher 验证 | **5 层** | Prompt → 格式 → 白名单 → EXPLAIN → 回译校验 |

> ⚠️ 已知评估缺口：缺少 RAG 检索质量评估、缺少 LLM-as-Judge 回答质量评估、缺少性能压测、缺少多轮对话收敛率统计。

---

## 五、工程全景

```
                    用户（患者/医生/管理员）
                          ↓
            ┌─────────────┴─────────────┐
            │  L2 接口层 FastAPI (9端点)   │
            └─────────────┬─────────────┘
                          ↓
            ┌─────────────────────────────┐
            │  L3 Supervisor Agent         │
            │  search_memory → 路由 → dispatch → save_memory  │
            └──┬──────┬──────┬──────┬─────┘
               ↓      ↓      ↓      ↓
          ┌─────────────────────────────┐
          │  L4 5个Agent                  │
          │  问诊 / 报告 / 药物 / 知识 / 运营   │
          └──┬──────┬──────┬──────┬─────┘
             ↓      ↓      ↓      ↓
          ┌─────────────────────────────┐
          │  L5 5个引擎模块 (7个文件)      │
          │  MedicalRAG / GraphRAG(Neo4j) / MS_GraphRAG │
          │  NL2SQL / VLM / MinerU / Memory  │
          └──┬──────┬──────┬──────┬─────┘
             ↓      ↓      ↓      ↓
          ┌─────────────────────────────┐
          │  L6 数据源层                  │
          │  10 MySQL表 + Neo4j图谱 + Milvus向量  │
          │  HIS/LIS/EMR/PACS 4适配器(模拟)│
          └──┬──────┬──────┬──────┬─────┘
             ↓      ↓      ↓      ↓
          ┌─────────────────────────────┐
          │  L7 模型层 (qwen3.7-max-2026-06-08)    │
          └─────────────────────────────┘
                          ↓
          ┌─────────────────────────────┐
          │  L8 数据治理 (审计/脱敏/RBAC)  │
          └─────────────────────────────┘
```

### 技术栈核心

| 类别 | 组件 | 实际版本 |
|------|------|------|
| Web 框架 | FastAPI + Starlette + Uvicorn | 0.139.2 / 1.3.1 / 0.34.0 |
| 关系数据库 | MySQL 8.0 + SQLAlchemy 2.0.30 | Docker: lingshu-mysql:15308 |
| 图数据库 | Neo4j 5.20 | 7 类节点 + 8 种关系 |
| 向量数据库 | Milvus 2.6.13 | DashScope text-embedding-v3, 1024 维 |
| 缓存 | Redis Stack 7.4.0-v3 | 短期记忆 Checkpointer |
| LLM | DashScope qwen3.7-max-2026-06-08 | 阿里云 API |
| Agent 框架 | LangChain 1.3.11 + LangGraph 1.2.7 | 7 节点 StateGraph + Supervisor+Worker |
| 文档解析 | MinerU 3.x (pipeline 后端) | PP-OCRv6 + 布局检测 + 公式/表格识别 |
| 知识图谱索引 | MS GraphRAG | Leiden 社区检测 + Local/Global Search |

---

## 六、高频追问 + 标准应答

| 面试官可能问 | 你怎么答 |
|-------------|---------|
| "你咋知道 Cypher 生成是对的呢？" | "5 层验证。第 3 层标签白名单是零成本纯正则，专门拦截 LLM 幻觉标签——因为 Neo4j 是 schema-optional，EXPLAIN 不校验标签是否存在。第 5 层回译校验是 soft-warning——不匹配时记录日志但不拦截，因为 LLM-as-Judge 本身可能出错。积累数据后再决定阈值。" |
| "MinerU 和 PyPDF2 有啥区别？" | "PyPDF2 提取文本流，不保留阅读顺序、不处理表格、不处理公式。临床检验报告里表格被读成乱序文本——WBC 下面写的是 12.5，但阅读顺序错乱后变成了另一个指标的值。MinerU 先做布局分析，把表格区域裁剪出来单独处理，输出保留原文格式的 Markdown。" |
| "Milvus 和 Neo4j 都存医学知识，冗余吗？" | "不是冗余，是分工。Milvus 做'语义翻译'——用户说'浑身没劲'→向量找到'乏力'（同义词匹配）；Neo4j 做'关系推理'——从'乏力'出发，找到对应疾病和常用药（图遍历）。两者是流水线上相邻工序。" |
| "多智能体同时调多个 API，超时怎么办？" | "**诚实地说，当前项目还没做熔断和超时控制**。这是已知缺限。企业方案：①各 Agent 独立超时 + 兜底回答；②Supervisor 设总超时；③关键路径（急诊检测）不走 LLM，用正则，不受 API 故障影响。" |
| "如果今天给你 100 份扫描版病历，你能处理吗？" | "MinerU pipeline 可以处理印刷体。但有两个前提：①扫描质量不能太差（150 DPI 以上）；②手写体 pipeline 准确率低。如果有 GPU，切到 vlm-engine 用视觉大模型处理手写体。pipeline 后端支持 GPU 但我们项目未配置——目前走纯 CPU。" |
| "你项目的评估数据怎么来的？" | "坦诚说，我们有 110 个单元测试 + 4 个评估维度（13 个评估测试）——覆盖了基础设施和确定性逻辑。但**缺少 Agent 回答质量的 LLM-as-Judge 评估和 RAG 检索质量的标注评估**——这些需要医生参与的标注数据集，是灰度上线前的必要工作。" |

---

## 七、建议的面试 PPT 结构（5 页核心）

| 页 | 标题 | 内容 |
|:--:|------|------|
| 1 | 数据全景图 | 一张图展示 HIS/LIS/PACS/EMR/CDSS 五种系统 + 各自的格式（SQL / DICOM / PDF / XML / 自由文本）→ 一个箭头指向统一处理入口 |
| 2 | 三层数据管道 | 结构化(SQL) → 半结构化(txt) → 非结构化(PDF/DICOM)，每种数据对应一种处理引擎（NL2SQL / RAG+GraphRAG / MinerU） |
| 3 | 单个难题深挖 | 选一个你最熟的（如 NL2Cypher 5 层验证或三层症状匹配管道），展开技术细节 + 对比方案 + 为什么选 A 不选 B |
| 4 | 量化指标 + 已知限制 | 左边列"已做到的"：110 测试 / 4 评估维度 / 5 层 Cypher 验证；右边列"诚实缺口"：无 RAG 评估、无熔断、无 LLM-as-Judge |
| 5 | 工程全景 | 8 层架构图 + 技术栈总表 + 一句话总结 |

---

## 八、诚实原则

在面试中主动说明以下已知限制不是弱点，而是**展示工程判断力**的机会：

| 已知限制 | 如何表述 |
|---------|---------|
| 无 RAG 检索评估 | "HyDE 实现了但没测——RAG 评估需要医生标注数据集，这是灰度上线前必须补的" |
| 无熔断机制 | "当前单机演示足够，但生产环境必须加——面试官您肯定遇到过 API 超时把整个请求挂住的情况" |
| NL2Cypher 回译校验是 soft-warning | "在数据不足时不设硬阈值是工程务实——LLM-as-Judge 假阳性可能误拦正确查询" |
| 适配器是模拟数据 | "生产环境替换为真实 HIS/LIS/PACS 的 HL7/FHIR/HTTP 接口，适配器模式保证替换成本可控" |
| 无性能压测 | "当前阶段目标是跑通全链路，压测是灰度上线前的验收任务" |
| MS GraphRAG 未实际运行 | "代码封装完备，配置也已就绪，但索引构建依赖 LLM API 调用约 20 分钟——首次运行时已解决所有配置兼容性问题" |

---

*本指南基于实际代码验证，代码路径：`D:\Courseware\YiLiaoProject\medical-agent`*
*所有技术指标均可在源码中找到对应实现，标注 ⚠️ 的为已知限制*
